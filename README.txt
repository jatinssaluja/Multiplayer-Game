Please find below the instructions to run multiplayer game locally.

Step-1: After downloading the project from Github, run “npm install” in the root directory of the project.

Step-2: Go to client directory, and run “npm install”.

Step-3: Go back to root directory using “cd ..”, and run “npm run dev”. Now, both client and server should be up and running locally.
